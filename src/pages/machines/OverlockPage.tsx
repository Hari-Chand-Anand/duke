import MachineCategoryPage from "./MachineCategoryPage";

export default function OverlockPage() {
  return (
    <MachineCategoryPage
      label="OUR MACHINES"
      title="OVERLOCK"
      desc="Trim + stitch + finish in one pass-clean edges, durable seams."
    />
  );
}
